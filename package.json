{
  "name": "abs-alexa-tests",
  "private": true,
  "description": "Test harness for the ABS-Alexa skill. Not deployed; the skill itself lives in lambda/.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "record": "node test/record.js"
  }
}
